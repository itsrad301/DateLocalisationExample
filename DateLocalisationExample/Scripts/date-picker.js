$(function () { // will trigger when the document is ready
        $('.date-picker').datepicker({
        dateFormat: 'dd-M-yy',
        //startDate: thisDate
    }

    ); //Initialise any date pickers
});